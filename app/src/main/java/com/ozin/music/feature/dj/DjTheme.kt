package com.ozin.music.feature.dj

import androidx.compose.ui.graphics.Color

/** DJ Mode's own dedicated matte-black/graphite controller look, visually
 * distinct from the main app's Material3 theme (deliberately, per spec — a
 * literal DJ controller shouldn't look like a settings screen). */
object DjColors {
    val panelBg = Color(0xFF121316)
    val panelSurface = Color(0xFF1B1D22)
    val panelSurfaceLight = Color(0xFF24272E)
    val deckA = Color(0xFF3E8CFF)
    val deckB = Color(0xFFFF6A3D)
    val ledGreen = Color(0xFF3DDC84)
    val ledOrange = Color(0xFFFF9E3D)
    val ledCyan = Color(0xFF3DE1E1)
    val textPrimary = Color(0xFFF1F2F5)
    val textSecondary = Color(0xFF9A9DA6)
    val knobTrack = Color(0xFF2E3138)
}
